===============================================
🎯 TASK
-----------------------------------------------
The dark-mode dashboard must look like the
attached screenshot: a single, solid navy
(#0C2836) canvas with **no thin white or grey
divider lines** anywhere.

===============================================
🔒 DO-NOT-TOUCH LIST
-----------------------------------------------
• Header items (avatar, EN/AR toggle)
• Sidebar layout, labels, icons, spacing
• Chat bar, prompt cards, fonts, animations
• Light-mode styles and colors
• Homepage / marketing pages

===============================================
🛠 WHAT TO FIX
-----------------------------------------------
1. **Remove stray borders**
   – Find and delete or recolor any 1 px borders,
     box-shadows, or outlines that appear as
     light lines against the dark background.
   – Typical culprits: Tailwind `border`,
     `divide-y`, or default `ring` utilities.

2. **Unify dark surfaces**
   – Background of header, sidebar, central
     panel, cards, and buttons = `#0C2836`
     (or the dark secondary `#1A2428` if a
     subtle surface separation is needed).
   – No lighter border between sidebar and
     central panel; rely on padding instead.

3. **Component audit**
   Check these components and ensure they
   show zero light lines in dark mode:
   • Sidebar & its active link highlight
   • Chat input field
   • Prompt cards & tab strip
   • Analytics charts / cards
   • Notifications list
   • Parties & Contacts page
   • Tags & Categories page

4. **RTL/LTR & Light mode untouched**
   – RTL flip must still work.
   – Light theme must remain exactly as now.

===============================================
✅ DONE CHECKLIST
-----------------------------------------------
☐ No visible white/grey divider lines in dark
  mode on any dashboard screen.
☐ Sidebar + header still fixed; routing intact.
☐ Light mode looks the same as before.
☐ Console shows: **DARK BACKGROUND CLEANED**
===============================================
