========================================================
ADD LIGHT & DARK THEMES — STICK TO CONTRAMIND COLORS
========================================================

🔒 A.  DO-NOT-TOUCH LIST (UI + ROUTING REMAIN AS IS)
----------------------------------------------------
• Header, sidebar, landing chat panel, animations, RTL/LTR behaviour — keep exactly the same.  
• Routing we just fixed must still work.  You’re adding *themes only*.

🎨 B.  BRAND COLOUR TOKENS (from the guidelines, p. 23-24)
----------------------------------------------------------
PRIMARY (dark mode bg)     #0C2836   “Dark Navy Blue”  
ACCENT                     #B7DEE8   “Sky Light Blue”  
NEUTRAL DARK               #101920   “Rich Black”  
NEUTRAL LIGHT              #E6E6E6   “Light Grey”  
Optional tints (see palette) for UI states:  
  • 700 #2B4F62   • 400 #60859B   • 100 #BED5E3 :contentReference[oaicite:0]{index=0}

---------------------------------------------------------
C.  TASKS
---------------------------------------------------------
1. **Create a theme switcher**  
   • Toggle icon (sun/moon) lives in the top-right header, left of the EN/AR switch.  
   • Persist choice in `localStorage` so the app re-opens in the user’s last theme.  

2. **Define colour tokens with CSS variables (or Tailwind config):**  
   `--cm-bg`, `--cm-surface`, `--cm-text`, `--cm-accent`, `--cm-border`.  
   Dark theme values:  
      `--cm-bg:#0C2836`, `--cm-surface:#101920`, `--cm-text:#FFFFFF`, `--cm-accent:#B7DEE8`, `--cm-border:#2B4F62`.  
   Light theme values:  
      `--cm-bg:#FFFFFF`, `--cm-surface:#F8F9FA`, `--cm-text:#0C2836`, `--cm-accent:#0C2836`, `--cm-border:#E6E6E6`.  
   All existing components must read colours from these variables (no hard-coded hex).

3. **Automatic chart re-colouring**  
   • Use the same accent (#B7DEE8) for bars/lines in both themes; axis/grid switch between `--cm-text` @ 60% opacity and `--cm-border`.  

4. **Accessibility check**  
   • Ensure text/foreground contrast ≥ 4.5:1 against the chosen background for both themes.  

5. **RTL parity**  
   • The toggle and all mirrored icons must flip correctly in Arabic view; otherwise untouched.

6. **QA checklist (must pass)**  
   ✅ Dark is default for first-time visitors.  
   ✅ Switching themes never reloads the page; sidebar/header stay put.  
   ✅ LocalStorage key `cm-theme` saves “dark” or “light”.  
   ✅ All pages (landing, analytics, parties, notifications, tags) inherit the theme without extra code.  
   ✅ No console errors. After testing log **`THEMES ENABLED`**.

DO NOTHING ELSE.  This task adds theme support using the exact ContraMind colours.
========================================================
